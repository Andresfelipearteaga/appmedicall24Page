---
import Layout from "../layouts/layoutGeneral.astro";
import InfoPlan from "../components/astro/planesPage/InfoPlan.astro";
import GetPlan from "../components/astro/planesPage/getPlan.astro";
import ViewIndex from "../components/astro/planesPage/ViewIndex.astro";
import InfoText from "../components/astro/planesPage/InfoText.astro";
import HeaderPlan from "../components/astro/planesPage/HeaderPlanes.astro";
import Simulated from "../components/astro/planesPage/Simulated.astro";
---

<main>
  <Layout title="Medicall241">
    <HeaderPlan />
    <section
      class="bg-gradient-to-r from-pink-700 to-pink-500 py-16 px-48 md:px-12"
    >
    
      <ViewIndex />
    </section>

    <section class="py-12 px-48 md:px-12">
        <div
        id="hidden-section-1"
        class="mt-6 mb-6 overflow-hidden max-h-0 transition-all duration-500 ease-in-out">
        <div class="p-4 bg-white">
            <Simulated />
        </div>
      </div>
      <InfoText />
    </section>
    <section class="py-12 px-48 md:px-12">
      <InfoPlan />
    </section>
    <section class="py-12 px-48 md:px-12">
        <GetPlan />
    </section>
  </Layout>
</main>

<style>
  .animate-slide-in {
    animation: slide-in 1s ease-out;
  }

  .animate-float-left {
    animation: float-left 3s ease-in-out infinite;
  }

  @keyframes float-left {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-10px);
    }
  }

  @keyframes slide-in {
    0% {
      opacity: 0;
    }
    100% {
      opacity: 1;
    }
  }

  @keyframes scrollUp {
    0% {
      transform: translateY(
        100%
      ); /* Empuja el contenido hacia abajo inicialmente */
    }
    100% {
      transform: translateY(0); /* El contenido vuelve a su posición original */
    }
  }
</style>
